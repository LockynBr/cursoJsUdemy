// for (let i = 0; i <=10; i++) {
//     const parImpar = (i % 2 === 0) ? `${i} Par` : `${i} Impar`;
//     console.log(parImpar);
// }

const frutas = ['Maça', 'Pêra', 'Uva'];

for (let i = 0; i < frutas.length; i++) {
    console.log(frutas[i]);
}